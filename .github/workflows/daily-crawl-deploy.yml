name: Daily Crawl and Deploy

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 1 * * *'  # 每天凌晨 1 点 UTC = 北京时间 9 点
  workflow_dispatch:       # 手动触发

jobs:
  crawl-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: |
          pip install feedparser requests beautifulsoup4
        working-directory: ./marketing-intelligence

      - name: Run crawler
        run: python scripts/crawl.py
        working-directory: ./marketing-intelligence

      - name: Generate Chinese summaries
        run: python scripts/improve_summaries.py
        working-directory: ./marketing-intelligence
        continue-on-error: true
        timeout-minutes: 20
        if: false  # SKIP FOR NOW - uncomment after Hugo build verified

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: '0.131.0'

      - name: Debug environment
        run: |
          echo "=== pwd ==="
          pwd
          echo "=== marketing-intelligence ==="
          ls -la marketing-intelligence/
          echo "=== layouts ==="
          ls -la marketing-intelligence/layouts/
          echo "=== data ==="
          ls -la marketing-intelligence/data/
        working-directory: .

      - name: Build Hugo site
        run: |
          pwd
          ls -la
          hugo --destination public 2>&1
        working-directory: ./marketing-intelligence

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public